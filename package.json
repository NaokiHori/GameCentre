{
  "name": "trash",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare": "husky .husky"
  },
  "lint-staged": {
    "!(*.ts)": [
      "prettier --write"
    ],
    "*.ts": [
      "prettier --write",
      "eslint --cache --fix"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.2",
    "prettier": "^3.4.2",
    "typescript-eslint": "^8.20.0"
  }
}
